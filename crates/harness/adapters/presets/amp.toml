name = "amp"
argv = ["amp", "-x", "{prompt}", "--stream-json", "--dangerously-allow-all"]
turn_cap = "none"
instruction_file = "AGENTS.md"

[usage]
input_tokens = "usage.input_tokens"
output_tokens = "usage.output_tokens"
