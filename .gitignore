target/

# Vendored skills are other people's work, fetched from harness.lock by `enallagi skills sync` and
# never redistributed here. `running-the-loop` is installed by `enallagi init` on dogfood branches.
.claude/skills/*
!.claude/skills/running-the-loop/

.DS_Store
